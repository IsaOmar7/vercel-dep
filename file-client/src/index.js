import React from 'react';
import ReactDOM from 'react-dom';
import UrlForm from './UrlForm'; // Ensure this path is correct

ReactDOM.render(
  <React.StrictMode>
    <UrlForm />
  </React.StrictMode>,
  document.getElementById('root')
);
